# Dependencias solo para desarrollo y pruebas
# Instalar con: pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.5
pytest-cov==6.1.0
httpx                 # requerido por el TestClient de starlette/fastapi
